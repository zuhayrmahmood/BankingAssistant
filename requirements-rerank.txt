# Optional: cross-encoder reranker backend (--rerank-backend cross-encoder).
#
# Installs the sentence-transformers / torch stack (~2 GB). Without these, the
# assistant falls back to the Ollama LLM reranker (--rerank-backend llm), which
# needs no extra dependencies. Install with:
#
#     pip install -r requirements-rerank.txt
#
# Note: transformers pulls tokenizers<0.23, downgrading the base pin. Install in
# a separate venv if that matters for other tooling.
sentence-transformers==5.6.0
torch==2.13.0
transformers==5.14.1
scikit-learn==1.9.0
scipy==1.18.0
